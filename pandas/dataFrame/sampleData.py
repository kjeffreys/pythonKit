'''
File to hold Python native and pandas data structures for sample data that may be reused in multiple files
'''
import pandas as pd

indexObjSeries = pd.Series(range(3), index=['a', 'b', 'c'])